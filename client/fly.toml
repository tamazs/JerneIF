# fly.toml app configuration file generated for jerneif-frontend on 2025-11-18T20:33:28+01:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'jerneif-frontend'
primary_region = 'fra'

[build]

[http_service]
  internal_port = 80
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']
  
[env]
  BACKEND_URL = "https://jerneif-backend.fly.dev"

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
